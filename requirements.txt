# core computation
duckdb>=1.0
pandas>=2.0
numpy>=1.24
pyarrow>=14.0

# app + charts (Phase 3)
streamlit>=1.50
altair>=5.0

# narrative layer (optional at runtime; only needed for live model generation)
anthropic>=0.40
openai>=1.30

# deck generation (monthly variance PowerPoint)
python-pptx>=1.0

# Word deliverables (flash, memo, budget-owner packets)
python-docx>=1.1

# tests
pytest>=7.0
